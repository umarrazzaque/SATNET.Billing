@using System.Globalization
@using SATNET.WebApp.Helpers

@{
    //if (!Request.IsAjaxRequest())
    //{
    //    Layout = SessionObjects.Theme.ThemePath + "/Layouts/_LayoutMain.cshtml";
    //}
    var controllerName = this.ViewContext.RouteData.Values["controller"].ToString();
    //var controller = HttpContext.Current.Request.RequestContext.RouteData.Values["controller"].ToString();
}
@{
    <section class="content">
        <div class="card">
            <!-- /.card-header -->
            <div class="card-body">
                <div class="row">
                    <a class="btn btn-primary right-pan" href="@String.Format("/{0}", "Package/Add")">
                        <span class='fa fa-plus' aria-hidden='true'></span> &nbsp;Edit
                    </a>
                </div>
                <div id="MainContent">
                    
                </div>
            </div>
            <!-- /.card-body -->
        </div>
    </section>
    @*if (SessionObjects.MenuBriefDescription != "")
    {
        <a href="#" onclick="Layout.Help(true)" class="helpbtn vertical-text"><i class="fa  fa-question-circle"></i> HELP</a>
        <aside class="helpdiv" id="helpdiv">
            <a href="#" onclick="Layout.Help(false)" class="closebtn vertical-text"><i class="fa fa-remove"></i> Close</a>
            @Html.Raw(SessionObjects.MenuBriefDescription)
        </aside>
    }*@
}

@*<section class="content-header">
    <h1>
        @SessionObjects.MenuHeading
        <small>@SessionObjects.MenuSubHeading</small>
    </h1>
    <ol class="breadcrumb">
        @if (SessionObjects.BredCrums != null)
        {
            var bcList = SessionObjects.BredCrums;
            var lastBc = SessionObjects.BredCrums.Last();

            foreach (var bc in bcList)
            {
                <li>
                    @{
                        var bUrl = Url.Content("~") + bc.Url;
                        if (bc.ParentId == 0)
                        {
                            //bUrl = Url.Action("SetModule", "Home", new { uid = SharedHelpers.Encrypt(bc.MenuId.ToString(CultureInfo.InvariantCulture)) });
                        }
                        if (lastBc == bc)
                        {
                            <i class="@bc.Icon"></i> @bc.DisplayName
                        }
                        else if (bc.CssClass == "")
                        {
                            <i class="@bc.Icon"></i> @bc.DisplayName
                        }
                        else
                        {
                            <a href="@bUrl" class="@bc.CssClass"><i class="@bc.Icon"></i> @bc.DisplayName</a>
                        }
                    }

                </li>
            }
        }
    </ol>
</section>*@

<div class="modal" id="deleteModal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body text-center">
                <i class="fa fa-warning text-danger delete-icon"></i>
                <p class="text-danger">Are you sure you want to delete this record?</p>
                <div class="col-md-4 col-md-offset-4">
                    <a href="" id="delModalAnch" data-ajax="true" data-dismiss="modal" class="btn btn-danger btn-flat" data-ajax-method="Post" data-ajax-loading="#divLoading" data-ajax-complete="Layout.actionComplete">Delete</a>
                    <button type="button" class="btn btn-default btn-flat" data-dismiss="modal">Cancel</button>
                </div>
                <div class="clearfix"></div>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>

<div class="modal" id="deleteModalParial">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body text-center">
                <i class="fa fa-warning text-danger delete-icon"></i>
                <p class="text-danger">Are you sure you want to delete this record?</p>
                <div class="col-md-4 col-md-offset-4">
                    <a href="" id="delModalAnchPartial" data-ajax="true" data-dismiss="modal" class="btn btn-danger btn-flat" data-ajax-method="Post" data-ajax-loading="#divLoading" data-ajax-complete="Layout.ReloadGrid">Delete</a>
                    <button type="button" class="btn btn-default btn-flat" data-dismiss="modal">Cancel</button>
                </div>
                <div class="clearfix"></div>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>
<div class="modal" id="restPasswordModal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body text-center">
                <i class="fa fa-warning text-warning delete-icon"></i>
                <p class="text-warning">Are you sure you want to proceed?</p>
                <div class="col-md-4 col-md-offset-4">
                    <a href="" id="restModalAnch" data-ajax="true" data-dismiss="modal" class="btn btn-warning btn-flat" data-ajax-method="Post" data-ajax-loading="#divLoading" data-ajax-complete="Layout.actionComplete">Proceed</a>
                    <button type="button" class="btn btn-default btn-flat" data-dismiss="modal">Cancel</button>
                </div>
                <div class="clearfix"></div>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>
<script>
    $(function () {
        $('.datepicker').datepicker({
            //startDate: '-3d',
            // format: "dd,MM,yyyy",
            format: "dd/MM/yyyy"
        });
    });
    //.datepicker("setDate", new Date())

</script>

